# Staging Environment Configuration
# Copy from .env.container and modify for staging

# Flask Configuration
FLASK_ENV=staging
DEBUG=true
SECRET_KEY=your-staging-secret-key-replace-this
APPROVAL_TOKEN_SECRET=your-staging-approval-secret-key-32-chars-min

# Database (will be overridden by compose.staging.yaml)
DATABASE_URL=postgresql+psycopg2://app_staging:staging_pass@localhost:5433/galveston_staging

# Email Configuration (use test email accounts)
BOOKING_APPROVAL_EMAIL=livingbayfront@gmail.com
BOOKING_NOTIFICATION_EMAILS=livingbayfront@gmail.com

# Application URLs
BASE_URL=http://83.229.35.162:8081

# Google Calendar (use staging calendar if available)
GOOGLE_CALENDAR_ID=livingbayfront@gmail.com
GOOGLE_CREDENTIALS_PATH=/run/secrets/service-account-staging.json

# Email SMTP Configuration
# IMPORTANT: Use MAIL_* variables (not SMTP_*) to match Flask-Mail configuration
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-staging-email@gmail.com
MAIL_PASSWORD=your-staging-app-password
MAIL_USE_TLS=true
MAIL_DEFAULT_SENDER=your-staging-email@gmail.com

# Security Settings
CSRF_SECRET_KEY=your-staging-csrf-secret-key
SESSION_COOKIE_SECURE=false  # Set to false for HTTP testing
SESSION_COOKIE_HTTPONLY=true

# Logging
LOG_LEVEL=DEBUG
LOG_FILE=/app/logs/staging.log
